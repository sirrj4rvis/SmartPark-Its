[pytest]
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing --cov-report=xml --cov-fail-under=85
markers =
    integration: tests requiring Docker (real Postgres via testcontainers)
filterwarnings =
    ignore::DeprecationWarning
